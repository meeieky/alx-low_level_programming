#include "main.h"

/**
 * _isupper - uppercase letters
 * @x: The number to be checked
 *
 * Return: 0 0r 1
 */

int _isupper(int x)

{
	
	if (x >= 'A' && x <= 'Z')
	{
		return (1);
	}
		return (0);
}
